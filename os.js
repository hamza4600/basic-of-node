// used for getting infoof system 
const os=require("os")
console.log(os.cpus()) // info of cpu processor 
console.table(os.freemem()) // frr memory
console.table(os.networkInterfaces()) // frr memory